import React from 'react';
import Header from '../components/Header';

interface AcademicProps { }

const Academic: React.FC<AcademicProps> = ({ }) => {
  return (
    <Header>
      <h1>Hello world Academic</h1>
    </Header>
  );
};

export default Academic;
